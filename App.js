/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 *
 * @format
 * @flow
 */

import React, { Fragment } from 'react';
import { StyleSheet, View, Text } from 'react-native';

const App = () => {
  return (
    <Fragment>
      <View>
        <Text>Hello to new app</Text>
      </View>
    </Fragment>
  );
};

const styles = StyleSheet.create({});

export default App;
